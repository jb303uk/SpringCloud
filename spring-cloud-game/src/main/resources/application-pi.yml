spring:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    hikari:
      maximum-pool-size: 4
      minimum-idle: 2
    url: jdbc:h2:file:~/h2-database/data
server:
  jetty:
    threads:
      max: 4
      min: 1
      idle-timeout: 10s
